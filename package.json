{
    "name": "db-project",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "setup": "node scripts/setup.js",
        "start": "concurrently -n backend,frontend -c blue,green \"npm run start:backend\" \"npm run start:frontend\"",
        "start:backend": "node scripts/run-backend.js",
        "start:frontend": "cd client && npm run build && npm run preview"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}
